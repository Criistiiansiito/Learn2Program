<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cursos Disponibles</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('fragments/navbar') %>

    <div class="container mt-4">
        <div class="header-container">
            <h1>Cursos Disponibles</h1>
            <div class="input-group">
                <input type="text" class="form-control" placeholder="Buscar...">
                <span class="input-group-text">🔍</span>
            </div>
        </div>

        <% cursos.forEach(curso => { %>
            <div class="card mb-3 course-card">
                <div class="card-body">
                    <h5 class="card-title"><%= curso.titulo %></h5>
                <div class="course-content">
                    <div>
                        <p class="card-text"><span class="label">LENGUAJE:</span> <%= curso.lenguaje %></p>
                        <p class="card-text"><span class="label">DIFICULTAD:</span> <%= curso.dificultad %>/10</p>
                        <p class="card-text"><span class="label">DURACIÓN ESTIMADA:</span> <% curso.duracion %> minutos</p>
                    </div>
                    <a href="#" class="btn btn-primary course-btn rounded-pill">Acceder al curso →</a>
                </div>
                </div>
            </div>
        <% }); %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <%- include('fragments/footer') %>
</body>
</html>